你是一个抖音逆向分析专家。下面是一份抖音 Aweme 对象的字段 dump JSON，每条字段包含 class（所在类）、field（字段名）、type（Java类型）、value（示例值）。

请对所有字段进行推测分析，按功能模块分组，输出一份 Markdown 文档。

## 输出要求

1. **文件命名**：`Aweme字段说明_v<抖音版本号>.md`，放在 `aweme_analysis/` 目录

2. **文档结构**：
   - 标题：`# Aweme 字段说明 (抖音 vX.X.X)`
   - 开头简述：版本、总字段数、class 层级
   - 按功能分大类（如：基础信息、视频、音乐、统计、电商、广告、推荐、直播、POI、安全、互动等）
   - 每个大类用表格：`| 字段 | 类型 | 示例值 | 所在类 | 推测含义 |`
   - 末尾附 **关键字段速查表**（高频使用字段的快速索引）

3. **推测原则**：
   - 根据字段名英文含义推测（如 `diggClicked` → 点赞点击状态、`playletTotalEpisode` → 短剧总集数）
   - 根据示例值辅助判断（`isAd: false` → 布尔标记、`city: "411600"` → 城市编码）
   - 根据 Java 类型判断（`boolean` → 开关/标记，`int` → 枚举/计数，`List` → 列表数据，`Object` → 子结构体）
   - 同类字段归入同一功能组（如 `teen*` → 青少年模式、`nearby*` → 附近、`commerce*`/`ecom*` → 电商）
   - `null` 值字段不能跳过，可能是条件性数据（如直播相关字段仅在直播 Aweme 中有值）
   - `hasXxxParsered` 类字段标注为**解析标记**，非业务数据
   - 常量大写字段（如 `ADAPTER`、`CREATOR`）标注为**内部常量**

4. **分类命名**用中文，顺序按重要性排列（基础信息 → 视频 → 音乐 → 统计 → 互动 → 电商 → 广告 → 推荐 → 直播 → POI → 安全 → 其他）

5. **速查表**放在文末，包含最常用的 20-30 个字段

## dump JSON 数据

（把 aweme_dump_vX.X.X.json 的内容粘贴在这里）
